import 'package:flutter/material.dart';

class InserteBoletoPage extends StatelessWidget {
  const InserteBoletoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Insert Boleto Page"),
      ),
    );
  }
}
